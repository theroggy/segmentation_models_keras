name: tests-minimal
channels:
  - conda-forge
dependencies:
  - python
  - pip
  - keras-applications>=1.0.7,<=1.0.8
  - keras
  - tensorflow
  - numpy <2
  # Linting, tests,...
  - pytest
  - pytest-cov
