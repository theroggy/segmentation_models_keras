name: tests-latest-tf-pip
channels:
  - conda-forge
dependencies:
  - python
  - pip
  - keras-applications>=1.0.7,<=1.0.8
  - keras
  - numpy <2
  # Linting, tests,...
  - pytest
  - pytest-cov
  - pip:
    - tensorflow
